#include <bits/stdc++.h>
using namespace std;

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
    
long long n, q, x, s=0; cin >> n >> q;
vector<long long> a(n+1);
for (long long i=0; i<n; i++){
    a[i]=s;
    cin >> x;
    s+=x;
}
a[n]=s;
for (long long i=0; i<q; i++){
    long long l, r; cin >> l >> r;
    cout << a[r]-a[l-1] << "\n";
   }
}
